---
- hosts: all
  become: yes
  gather_facts: yes
  tasks:
  - name: install webserver on redhat nodes
    dnf:
     name: httpd
     state: present

  - name: start webserver on redhat nodes
    service:
     name: httpd
     state: started
     


